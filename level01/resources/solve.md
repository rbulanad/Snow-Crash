1. Show all users with `getent passwd`

2. "flag01" line is different --> hashed password is visible

3. Use John The Ripper (install on home machine, git clone on 42's PC) to decrypt hashed password == abcdefg

4. `su flag01` + password == abcdefg

5. getflag
