<h2>🗡Caesar Cipher</h2>

1. Use command `find / -user "flag00" 2>/dev/null` to look for all files owned by specific user (2>/dev/null = don't show 'permission denied' results)

2. `cat` the result "/usr/sbin/john" == cdiiddwpgswtgt

3. pass the contents to decoder (dcode.fr) == nottoohardhere

4. `su flag00` + password == nottoohardhere

5. getflag
